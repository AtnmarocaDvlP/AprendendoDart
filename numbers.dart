import "dart:math";

void main (){
  
  int item1 = 300;
  int item2 = 100;
  double price = 2.99;
  
    print(
      5 + 3 * 4 - (4 / 2)
    ); 
  
    item1 += 25;
  // adiciona um valor a variavel atual de forma simples
  
    print(
      item1
    );
  
    item2++;
  // adiciona 1 para a variavel contavel
  
    print(
      item2
    );
  
    print(
      min(10, 9) 
      // imprime o menor numero entre eles (utiliza o (darth:math))
    );
    
    print(
      sqrt(144)
       // imprime a raiz quadrada (utiliza o (darth:math))
    );
}